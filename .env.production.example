# Database Configuration
DATABASE_URL="postgresql://username:password@host:port/database?schema=eventsvc"
DIRECT_URL="postgresql://username:password@host:port/database?schema=eventsvc"

# Server Configuration
PORT=4004
NODE_ENV="production"

# Service URLs
AUTH_SERVICE_URL="https://nexus-auth-service.vercel.app"
PROFILE_SERVICE_URL="https://nexus-profile-service.vercel.app"

# JWT Configuration (for verification)
JWT_ISSUER="nexus-auth-service"
JWT_AUDIENCE="nexus-platform"

# Optional: Redis for Caching
REDIS_URL="redis://username:password@host:port"

# Socket.IO Configuration (limited in serverless)
SOCKET_ENABLED=false

# Badge System Configuration
BADGE_UNLOCK_COUNT=8
DEFAULT_BADGES="Team Player,Leadership,Innovation,Problem Solver,Research Excellence,Community Impact,Outstanding Presentation,Top Contributor"
